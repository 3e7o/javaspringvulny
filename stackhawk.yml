app:
  applicationId: ${APP_ID}
  env: ${APP_ENV}
  host: ${APP_HOST:https://localhost:9000}
  waitForAppTarget:
    path: /
    waitTimeoutMillis: 200000
    pollDelay: 500
  excludePaths:
    - "/logout"
  antiCsrfParam: "_csrf"

